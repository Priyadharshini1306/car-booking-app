server.port=${PORT:8080}
# =========================
# PostgreSQL (Render DB)
# =========================
spring.datasource.url=jdbc:postgresql://dpg-d7hqhjkvikkc73aejbng-a:5432/carbooking_evt6
spring.datasource.username=carbooking_evt6_user
spring.datasource.password=WUlBSGh1dTkVmtksy5spGinYBnprlnNM
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# =========================
# JWT
# =========================
jwt.secret=${JWT_SECRET}
jwt.expiration=86400000

# =========================
# Email (Gmail SMTP)
# =========================
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USER}
spring.mail.password=${MAIL_PASS}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# =========================
# Admin Email
# =========================
admin.email=${ADMIN_EMAIL}

# =========================
# Actuator
# =========================
management.endpoints.web.exposure.include=health,info

# =========================
# Logging
# =========================
logging.level.com.carbooking=INFO